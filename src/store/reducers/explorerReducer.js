import explorerData from "../../data/explorerData";
import { INSERT_NODE } from "../actions/actionTypes";

const explorerReducer = (state = explorerData, action) => {
    switch (action.type) {
        case INSERT_NODE:
            
            break;

        default:
            break;
    }
}